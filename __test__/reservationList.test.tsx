describe('ReservationList Component', () => {
  it('예약 API에서 가져온 리뷰들의 데이터가 올바르게 렌더링되는지 확인', async () => {});

  it('예약 리스트에서 예약 클릭 시 조회수 증가 로직이 실행되는지 확인', async () => {});

  it('예약 리스트에서 예약 클릭 시 "/reservation/list/[id]" navigate가 호출되는지 확인', async () => {});

  it('비밀글 안보기 버튼을 눌렀을 때 비밀글이 아닌 예약글만 보이는지 확인', async () => {});

  it('비밀글 안보기 버튼을 다시 눌렀을 때 모든 예약글이 보이는지 확인', async () => {});
});
